# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4          # 目前还是 v4（v5/v6 可能有 breaking change，建议先用 v4 最新 patch）

      - name: Set up JDK 17
        uses: actions/setup-java@v5        # ← 升级到 v5（已支持 Node 24）

      - name: Run Serenity Tests
        run: mvn clean verify serenity:aggregate -Dfile.encoding=UTF-8

      - name: Upload Serenity Report
        uses: actions/upload-artifact@v4   # v4 目前仍然是主流推荐（v6 变化较大）
        if: always()
        with:
          name: serenity-full-report
          path: target/site/serenity/
          retention-days: 30